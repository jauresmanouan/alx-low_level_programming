#include "main.h"

/**
 * print_square - "#" use it to print some design
 * @size: numer of times
 */

void print_square(int size)
{
int i = 0;
while (i < size)
{
for (; i < size; i++)
_putchar(35);
_putchar(10);
i++;
}
}
