#include "main.h"

/**
 * mul - multiple 2 number and give the result
 * @a: first number
 * @b: second number
 * Return: the result
 */

int mul(int a, int b)
{
int result;
result = a * b;
return (result);
}
