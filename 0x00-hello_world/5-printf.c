#include<stdio.h>

/**
 * main - use printf to display message
 *
 * Return: 0 if everything is nice
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
